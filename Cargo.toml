[workspace]
resolver = "2"

members = [
    "lib",
    "standalone",
]
default-members = ["standalone"]

[workspace.dependencies]
eframe = { version = "0.21", features = ["dark-light", "wgpu"] }

[profile.release]
lto = "thin"
panic = "abort"
